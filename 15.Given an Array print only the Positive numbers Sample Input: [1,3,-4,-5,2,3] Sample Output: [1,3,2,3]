#include<stdio.h>
int main(){
    
    int n,arr[10],first=1;
    scanf("%d",&n);
    
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    
    for(int i=0;i<n;i++){
        if(arr[i] > 0){
            if(!first){
            printf(",");
        }
        printf("%d",arr[i]);
        first =0;
    }
    }
    return 0;
    
}
