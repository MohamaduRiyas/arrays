#include<stdio.h>
int main(){
  int n,i,arr[5],small;
  scanf("%d",&n);

  for(i=0;i<n;i++){
    scanf("%d",&arr[i]);
  }

  small = arr[0];

  for(i=1;i<n;i++){

      if(arr[i] < small){

        small = arr[i];
}
    
  }
printf("%d is smaller",small);

}
